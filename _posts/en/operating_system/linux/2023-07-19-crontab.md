---
title: "Automating Linux Tasks with Cron (Crontab)"
description: "How to use Linux crontab to run tasks automatically at specific times, along with the configuration rules"
excerpt: "A summary of cron usage including crontab installation, configuration rules, and running scripts from a Python virtual environment"
date: 2023-07-19
categories: Linux
tags: [Ubuntu, crontab, cron, automation, scheduler, Python, task-scheduling]
ref: crontab
---

:bulb: This guide covers how to use Cron (crontab) to run desired tasks automatically at specific times.
{: .notice--info}

# [01] What is Cron?

> A time-based job scheduler on Linux. Use it when you need to run a task periodically at a specific time, date, or interval. Configure and run jobs using `crontab`.

# [02] Install

```shell
sudo apt-get install update
sudo apt-get install cron
```

# [03] Commands

```shell
# View the current user's crontab
crontab -l

# Edit the current user's crontab
crontab -e

# Remove the current user's crontab
crontab -r

# ex)
root@emaster:~# crontab -l
no crontab for root
root@master:~# crontab -e
no crontab for root - using an empty one
crontab: installing new crontab

root@master:~# crontab -l
# Edit this file to introduce tasks to be run by cron.
#
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
#
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
#
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
#
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
#
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
#
# For more information see the manual pages of crontab(5) and cron(8)
#
# m h  dom mon dow   command

51 14 19 * * /root/code/.venv/bin/python3 /root/code/test.py
```

![2023-07-19 14 50 20](https://github.com/cmaven/cmaven.github.io/assets/76153041/39c5b507-bf71-4074-8b76-259ddfa35b78)

# [04] Configuration (Rules)

```shell
*  *  *  *  *   command
m  h  dom mon dow
```

- minute (0-59), hour (0-23), day of month (1-31), month (1-12), day of week (0-6: 0 = Sunday, 6 = Saturday)

```shell
# On the 19th of every month, at 14:51,
# run the /root/code/test.py program
# inside the /root/code/.venv/bin/python3 Python virtual environment
51 14 19 * * /root/code/.venv/bin/python3 /root/code/test.py
```

:small_blue_diamond:Reference: [Verify schedule timing (crontab.guru)](https://crontab.guru/){:target="_blank"}

# [05] Service Control

```shell
# Check status
service cron status

# Start
service cron start

# Stop
service cron stop
```

# [06] Extra: Running from a Python Virtual Environment

When you want a Python script to run inside a virtual environment, include the shell shebang line at the top of the Python file.

```python
#! /root/code/.venv/bin/python3 /root/code/test.py
print("Hello")
```
